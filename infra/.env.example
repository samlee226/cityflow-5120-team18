# Template only. See infra/README.md for the commands that copy this file and
# substitute a generated password. The placeholder below must be replaced in
# both values, and the two must match, or the database will reject the
# connection string.
#
# .env is gitignored. Never commit real credentials.
POSTGRES_DB=cityflow
POSTGRES_USER=cityflow_app
POSTGRES_PASSWORD=CHANGE_ME

# Connection string for the pipeline and the API. The database listens on
# loopback only, so this is localhost from the instance's point of view, and
# also from a workstation with the SSH tunnel open.
DATABASE_URL=postgresql://cityflow_app:CHANGE_ME@localhost:5432/cityflow

# Live ingestion retention. The systemd service reads these values from the
# copied infra/compose/.env file after each successful non-dry-run ingestion.
CITYFLOW_LIVE_RETENTION_HOURS=24
CITYFLOW_LIVE_QUARANTINE_RETENTION_DAYS=7
CITYFLOW_LIVE_RUN_RETENTION_DAYS=30
