# Copy this to .env and fill in real values. .env must be gitignored --
# never commit real credentials.
DATABASE_URL=postgresql://fit5120_app:CHANGE_ME@localhost:5432/fit5120
DB_POOL_MIN_SIZE=1
DB_POOL_MAX_SIZE=10
